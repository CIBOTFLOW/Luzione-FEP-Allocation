{
  "name": "luzione-fep-allocation",
  "version": "0.7.0-draft",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/*.js scripts/*.js && node --check public/app.js && npm test",
    "proof:b07": "node scripts/b07Proof.js"
  },
  "engines": {
    "node": ">=20"
  }
}
