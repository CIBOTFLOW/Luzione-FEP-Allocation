{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.luzione.example/luzione-movement-interaction-v0.1-draft.schema.json",
  "title": "Luzione movement interaction request v0.1 draft",
  "type": "object",
  "additionalProperties": false,
  "required": ["action"],
  "properties": {
    "action": { "enum": ["LIKE", "SAVE", "SEND", "FOLLOW"] },
    "accountId": { "type": "string", "minLength": 1, "maxLength": 200 }
  }
}
