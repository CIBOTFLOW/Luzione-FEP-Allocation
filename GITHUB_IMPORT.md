# GitHub Import

Repository: `CIBOTFLOW/Luzione-FEP-Allocation`

1. Clone the empty repository.
2. Create branch `feat/fep-allocation-v0.3`.
3. Copy this package into the root.
4. Run `npm test`, `npm run check`, then `npm start` for the executable prototype.
5. Paste `CODEX_MASTER_PROMPT.md` into the connected Codex thread.
6. Replace the prototype with the production Next.js portal while preserving policy tests.
7. Connect only to FEP Platform's sponsor APIs. Never place Supabase service credentials in the browser.
